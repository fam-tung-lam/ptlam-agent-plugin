# Project agent instructions

Never read/write in [skills](skills), as it is generated.
The source of truth for agent plugin is the folder [plugin](plugin).

The plugin is pre-release. Remove a skill outright when it is merged, replaced,
or renamed. Do not use `status: deprecated` or `status: archived`.

<!-- PTLAM-INIT:START -->
## PTLam preferences

Always read and apply [agent override instructions](AGENTS.override.md)
before following the rest of this file. When they conflict, prefer
`AGENTS.override.md` over `AGENTS.md`.
<!-- PTLAM-INIT:END -->
